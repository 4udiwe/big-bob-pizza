# 🤝 Руководство по вкладу в проект

Общие правила для вклада в проект Big Bob Pizza.

---

## 🧱 1. Общие принципы

- Основная ветка — `main`.  
  В неё **нельзя пушить напрямую**.
- Все изменения добавляются через **Pull Request (PR)**.
- Один PR = одна логическая задача или фича.
- Если работа не завершена — создавайте **Draft PR**.
- Следите, чтобы код компилировался и проходил все тесты перед мержем.

---

## 🌿 2. Работа с ветками

Перед началом работы:
```
git checkout main
git pull origin main
```

Создайте новую ветку:
```
git checkout -b feature/<название_задачи>
```

Примеры:
```
feature/order-service

feature/payment-service

fix/cart-calculation

refactor/database-layer
```

После завершения работы:
```
git add .
git commit -m "feat: add order service basic logic"
git push origin feature/order-service
```

## 🧩 3. Формат коммитов
Старайтесь писать осмысленные коммиты в едином стиле:

| Тип         | Назначение                                 | Пример                                  |
| ----------- | ------------------------------------------ | --------------------------------------- |
| `feat:`     | Новая функциональность                     | `feat: implement order creation`        |
| `fix:`      | Исправление ошибок                         | `fix: correct total amount calculation` |
| `refactor:` | Рефакторинг без изменения логики           | `refactor: simplify order handler`      |
| `docs:`     | Документация                               | `docs: update README for kafka setup`   |
| `test:`     | Тесты                                      | `test: add order repository tests`      |
| `chore:`    | Вспомогательные задачи (CI, Docker и т.д.) | `chore: update docker-compose`          |

## 🔀 4. Pull Request
После пуша ветки создайте Pull Request в main:

Зайдите на страницу репозитория.
Нажмите “Compare & Pull Request”.

Укажите:

- Base branch: main
- Compare branch: ваша ветка (feature/...)

Заполните описание:

- кратко, что сделано;
- зачем это нужно;
- как проверить.

**Если задача не закончена, выберите Draft PR.**

## 👀 5. Code Review
Каждый PR должен быть просмотрен хотя бы одним участником команды.

При необходимости оставляйте комментарии в коде.

После исправлений отметьте их как resolved.

После одобрения:

используйте Squash & Merge, чтобы объединить коммиты в один чистый.

GitHub автоматически предложит удалить ветку после merge — соглашайтесь, если она больше не нужна.

## 🧱 6. Branch Protection (защита ветки)
Ветка main защищена, и в неё нельзя пушить напрямую.
Все изменения должны проходить через PR и (по мере внедрения) автоматические проверки.

## ⚙️ 7. Общие контракты
Каждый микросервис разрабатывается независимо, но использует общие принципы.

Обращайте внимание на общие ресурсы: Kafka topics, API контракты. (Будут добавлены позже в файл ARCHITECTURE.md)

**Если ваш PR затрагивает межсервисное взаимодействие — обязательно обновите соответствующие описания.**

## 🧪 8. Проверка перед PR
Перед созданием PR убедитесь, что:

проект собирается (go build ./...);

тесты проходят (go test ./...);

линтер не ругается (golangci-lint run или аналог);

не оставлены временные логи и закомментированный код.

## 🗂️ 9. Структура репозитория (рекомендация)
Рекомендуется добавить которкий README.md внутри каждого сервиса.

Общие файлы репозитория, которые лежат в корне:
- .gitingore (Дополняем по необходимости, распространяется на все поддиректории)
- docker-compose.yaml
- .env (Также дополняем по необходимости)
- Makefile
- README.md

Файлы, специфичные для каждого сервиса:
- Dockerfile
- .env (опционально)
- Makefile (опционально)

## 💬 10. Коммуникация
Перед началом крупной задачи обсудите её с командой.

Все архитектурные решения будем фиксировать в  /docs/ARCHITECTURE.md.

Вопросы и предложения приветствуются, пишите в коментарии к PR.
